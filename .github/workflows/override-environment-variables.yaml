name: Override environment variables
on: push
env:
  EXAMPLE: workflow level
jobs:
  run:
    runs-on: ubuntu-latest
    steps:
      - run: echo "${EXAMPLE}"
      - env:
        EXAMPLE: steps level
        run: echo "${EXAMPLE}"